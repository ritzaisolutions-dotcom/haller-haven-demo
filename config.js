window.RAIS_ADMIN = { tenant: "haller-haven" };
window.RAIS_STORE_KEY = "rais-listings-haller-haven";
/** Canonical site origin — replace at go-live (sitemap.xml, robots.txt, llms.txt, HTML canonicals too). */
window.RAIS_SITE_BASE = "https://haller-haven-demo.vercel.app";
/** Max listings shown on the home page (featured only). */
window.RAIS_HOME_MAX = 6;
/** Live listings endpoint (falls back to /listings.json when API unavailable). */
window.RAIS_LISTINGS_URL = "/api/listings";
